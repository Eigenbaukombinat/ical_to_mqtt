# ical alarms to mqtt

## Usage


```bash
mkdir ical2mqtt
cd ical2mqtt
git clone git@github.com:Eigenbaukombinat/ical_to_mqtt.git
python3 -m venv .
bin/pip install ical_to_mqtt/
bin/ical2mqtt --help
```

